#include<stdio.h>
int main()
{
    float length, breadth;
    printf("what is the length of rectangle\n");
    scanf("%f", &length);
    printf("what is the breadth of a rectangle\n");
    scanf("%f", &breadth);
    printf("The Area of rectangle is %f", length*breadth);
    return 0;
}
