#include<stdio.h>
int main()
{
    //area of a square
    int side;
    printf("enter the side of square\n ");
    scanf("%d", &side);
    int area = side * side;
    printf("the area of square is %d", area);
    return 0;

}
